print("Hello World!")
setwd("D:/Seba Privat/Machine Learning A-Z/Part 2 - Regression/Section 5 - Multiple Linear Regression")
source('D:/Seba Privat/Machine Learning A-Z/Part 2 - Regression/Section 5 - Multiple Linear Regression/Multiple linear regression.r', echo=TRUE)
View(dataset)
View(dataset)
source('D:/Seba Privat/Machine Learning A-Z/Part 2 - Regression/Section 5 - Multiple Linear Regression/Multiple linear regression.r', echo=TRUE)
dataset = read.csv('50_Startups.csv')
View(dataset)
# Encoding categorical data
dataset$Country = factor(dataset$State,
levels = c('California', 'Florida', 'New York'),
labels = c(1, 2, 3))
# Importing the dataset
dataset = read.csv('50_Startups.csv')
# Encoding categorical data
dataset$State = factor(dataset$State,
levels = c('California', 'Florida', 'New York'),
labels = c(1, 2, 3))
# Multiple linear regression
# Importing the dataset
dataset = read.csv('50_Startups.csv')
# Encoding categorical data
dataset$State = factor(dataset$State,
levels = c('California', 'Florida', 'New York'),
labels = c(1, 2, 3))
# Splitting the dataset into the Training set and Test set
# install.packages('caTools')
library(caTools)
set.seed(123)
split = sample.split(dataset$DependentVariable, SplitRatio = 0.8)
training_set = subset(dataset, split == TRUE)
test_set = subset(dataset, split == FALSE)
# Feature Scaling
# training_set = scale(training_set)
# test_set = scale(test_set)
set.seed(123)
split = sample.split(dataset$DependentVariable, SplitRatio = 0.8)
training_set = subset(dataset, split == TRUE)
test_set = subset(dataset, split == FALSE)
split = sample.split(dataset$Profit, SplitRatio = 0.8)
training_set = subset(dataset, split == TRUE)
test_set = subset(dataset, split == FALSE)
View(training_set)
View(test_set)
# Fitting multiple linear regression to the training set
# the "." in the formular is short for "R.D.Spend + Adminitration + Marketing.Spend + State"
regressor = lm(formular = Profit ~ .,
data = training_set)
regressor = lm(formula = Profit ~ .,
data = training_set)
summary(regressor)
View(regressor)
View(regressor)
regressor = lm(formula = Profit ~ R.D.Spend,
data = training_set)
summary(regressor)
regressor = lm(formula = Profit ~ R.D.Spend + Marketing.Spend,
data = training_set)
summary(regressor)
# Predicting the testset results
y_pred = predict(regressor, newdata = test_set)
y_pred
View(test_set)
View(test_set)
# Fitting multiple linear regression to the training set
# the "." in the formular is short for "R.D.Spend + Adminitration + Marketing.Spend + State"
regressor = lm(formula = Profit ~ .,
data = training_set)
# Predicting the testset results
y_pred = predict(regressor, newdata = test_set)
y_pred
source('D:/Seba Privat/Machine Learning A-Z/Part 2 - Regression/Section 5 - Multiple Linear Regression/Multiple linear regression.r', echo=TRUE)
# Fitting multiple linear regression to the training set
# the "." in the formular is short for "R.D.Spend + Adminitration + Marketing.Spend + State"
regressor = lm(formula = Profit ~ R.D.Spend + Marketing.Spend,
data = training_set)
# Console: summary(regressor)
# Predicting the testset results
y_pred = predict(regressor, newdata = test_set)
# install.packages('caTools')
library(caTools)
set.seed(123)
split = sample.split(dataset$Profit, SplitRatio = 0.8)
training_set = subset(dataset, split == TRUE)
test_set = subset(dataset, split == FALSE)
print(summary(regressor))
# Fitting multiple linear regression to the training set
# the "." in the formular is short for "R.D.Spend + Adminitration + Marketing.Spend + State"
regressor = lm(formula = Profit ~ R.D.Spend + Marketing.Spend,
data = training_set)
print(summary(regressor))
# Predicting the testset results
y_pred = predict(regressor, newdata = test_set)
print(y_pred)
# Fitting multiple linear regression to the training set
# the "." in the formular is short for "R.D.Spend + Adminitration + Marketing.Spend + State"
regressor = lm(formula = Profit ~ R.D.Spend + Marketing.Spend,
data = dataset)
print(summary(regressor))
# Predicting the testset results
y_pred = predict(regressor, newdata = test_set)
print(y_pred)
# Fitting multiple linear regression to the training set
# the "." in the formular is short for "R.D.Spend + Adminitration + Marketing.Spend + State"
regressor = lm(formula = Profit ~ R.D.Spend + Marketing.Spend,
data = dataset)
summary(regressor)
# Predicting the testset results
y_pred = predict(regressor, newdata = test_set)
print(y_pred)
